SELECT
  SKILLS.NAME            AS SKILL,
  SUM(DEVELOPERS.SALARY) AS SUM_SALARY
FROM DEVELOPERS, DEVELOPERS_SKILLS, SKILLS
WHERE
  DEVELOPERS.ID = DEVELOPERS_SKILLS.DEVELOPER_ID
  AND DEVELOPERS_SKILLS.SKILL_ID = SKILLS.ID
  AND SKILLS.NAME LIKE 'Java'
GROUP BY SKILLS.NAME;